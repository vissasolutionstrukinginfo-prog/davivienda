[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "node start-prod.cjs"
healthcheckPath = "/api/settings/mobile_app_enabled"
healthcheckTimeout = 350
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
